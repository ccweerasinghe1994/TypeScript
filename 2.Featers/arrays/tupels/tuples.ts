const drink = {
  color: "brown",
  carbonated: true,
  suger: 40
};

type drink = [string, boolean, number];

const pepsi: drink = ["brown", true, 40];
const sprite: drink = ["green", true, 50];

const carSpecs: [number, number] = [2300, 230];

const carspecs = {
  hoesePower: 2300,
  weight: 230
};
